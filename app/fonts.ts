import { Crimson_Pro } from "next/font/google";

/** Paper letter face, shared by its condensed card and full-size overlay. */
export const crimsonPro = Crimson_Pro({
  subsets: ["latin"],
  display: "swap",
});
